$body = @{
    scenarioId = "scenario-C"
    copilotAnswer = "Noise levels are within safe limits. Continue monitoring."
} | ConvertTo-Json

Invoke-RestMethod `
    -Uri "http://localhost:5000/api/report" `
    -Method Post `
    -ContentType "application/json" `
    -Body $body